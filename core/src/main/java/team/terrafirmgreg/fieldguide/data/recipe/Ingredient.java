package team.terrafirmgreg.fieldguide.data.recipe;

/**
 * Marker for recipe ingredient POJOs.
 *
 * <p>Lives in :core as pure schema — rendering hints (icons, fluid bucket
 * fallback, etc.) belong to consumer modules (cli, forge htmlrender). All
 * concrete subclasses live under {@code data.recipe.ingredient}.</p>
 */
public interface Ingredient {
}
